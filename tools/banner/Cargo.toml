[package]
name = "ditherlib-banner"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
publish = false

[dependencies]
ditherlib = { path = "../.." }
image = { version = "0.25.10", default-features = false, features = ["jpeg", "png"] }
